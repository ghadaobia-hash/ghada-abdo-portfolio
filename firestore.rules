rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for portfolio JSON. Writes open for demo — replace with Firebase Auth before production.
    match /siteContent/{docId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
